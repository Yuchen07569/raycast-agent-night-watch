# Privacy

Agent Night Watch has no analytics, telemetry, accounts, advertising, or
network requests. It reads local macOS power state and stores only temporary
session ownership data in the user's cache directory.

Administrator credentials remain inside the macOS authorization dialog and
are never received by the extension.
